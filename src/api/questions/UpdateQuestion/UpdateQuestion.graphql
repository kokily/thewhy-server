type UpdateQuestionResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  UpdateQuestion(
    id: ID!
    password: String!
    name: String
    phone: String
    email: String
    title: String
    body: String
  ): UpdateQuestionResponse!
}
